# 🚨 ВАЖНО! Настройка базы данных

## ⚠️ ОБЯЗАТЕЛЬНЫЙ ШАГ!

Чтобы синхронизация работала между устройствами, нужно **ОДИН РАЗ** выполнить SQL скрипт в Supabase.

---

## 📝 Пошаговая инструкция:

### Шаг 1: Откройте Supabase
Перейдите по ссылке: https://supabase.com/dashboard/project/bnfigclshxuarnryvhma

### Шаг 2: Откройте SQL Editor
В левом меню найдите и нажмите **"SQL Editor"**

### Шаг 3: Создайте новый запрос
Нажмите кнопку **"New query"** (или "+ New Query")

### Шаг 4: Скопируйте SQL скрипт
Откройте файл **`supabase-setup-multiuser.sql`** из вашего проекта

### Шаг 5: Вставьте в редактор
Скопируйте **ВСЁ содержимое** файла и вставьте в SQL Editor

### Шаг 6: Выполните скрипт
Нажмите зеленую кнопку **"Run"** (или Ctrl+Enter / Cmd+Enter)

### Шаг 7: Проверьте результат
Должно появиться: ✅ **"Success. No rows returned"** или подобное сообщение

---

## 🔍 Проверка что всё создалось:

### 1. Откройте Table Editor
В левом меню нажмите **"Table Editor"**

### 2. Проверьте таблицы
Должны появиться **5 таблиц**:
- ✅ `budget_settings` - настройки бюджета
- ✅ `base_expenses` - базовые расходы
- ✅ `categories` - категории
- ✅ `transactions` - история операций
- ✅ `goals` - цели (новое!)

### 3. Проверьте данные
Откройте каждую таблицу и убедитесь что есть начальные данные для **двух пользователей**:
- `user_krasotka`
- `user_svyatik12`

---

## ✅ Что даст выполнение скрипта:

После выполнения SQL скрипта:

1. **🔄 Синхронизация в реальном времени**
   - Добавили расход на телефоне → сразу видно на компьютере
   - Изменили категорию на компьютере → обновилось на телефоне
   - Создали цель → синхронизировалась везде

2. **👥 Раздельные данные**
   - У каждого пользователя свой бюджет
   - Данные не пересекаются
   - Полная изоляция

3. **💾 Облачное хранение**
   - Данные в безопасности
   - Не потеряете при очистке браузера
   - Доступ с любого устройства

4. **🎯 Поддержка целей**
   - Создавайте финансовые цели
   - Отслеживайте прогресс
   - Мотивируйтесь!

---

## ❗ Если что-то пошло не так:

### Ошибка: "Table already exists"
**Решение:**
1. Это нормально, если вы уже запускали старый скрипт
2. Сначала удалите старые таблицы:
```sql
DROP TABLE IF EXISTS transactions CASCADE;
DROP TABLE IF EXISTS categories CASCADE;
DROP TABLE IF EXISTS base_expenses CASCADE;
DROP TABLE IF EXISTS budget_settings CASCADE;
```
3. Затем запустите весь скрипт заново

### Ошибка: "Publication doesn't exist"
**Решение:**
Уберите строки с `ALTER PUBLICATION` и выполните только создание таблиц

### Синхронизация не работает
**Проверьте:**
1. ✅ SQL скрипт выполнен без ошибок
2. ✅ Все 5 таблиц созданы в Table Editor
3. ✅ Есть начальные данные для обоих пользователей
4. ✅ Приложение перезагружено (Ctrl+R / Cmd+R)

---

## 🎉 После выполнения:

1. **Перезагрузите приложение** на всех устройствах (F5 / Cmd+R)
2. Войдите под своим пользователем
3. Измените что-нибудь (например, доход)
4. Откройте на другом устройстве
5. **Должно синхронизироваться автоматически!** ✨

---

## 💡 Совет:

Выполните скрипт **прямо сейчас** - это займет 2 минуты, и всё заработает!

**После выполнения напишите разработчику "SQL выполнен" для подтверждения.** ✅

---

Создано с ❤️ для бесшовной синхронизации между устройствами


